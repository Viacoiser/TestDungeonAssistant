/**
 * CharacterInspect.jsx
 * Thin wrapper that delegates to the new CharacterSheet5e component.
 * Keeps the same props API so CampaignView / Dashboard don't need changes.
 */
import CharacterSheet5e from './sheet/CharacterSheet5e'
export default CharacterSheet5e
